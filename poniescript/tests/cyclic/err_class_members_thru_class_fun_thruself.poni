fun evil(e: Example) -> int {
    return e.a1 + 20;
}

class Example {
    // This is invalid because evil() reads from a1.

    // TODO: This test case currently passes... Uh oh.

    // -- this error is no longer applicable, because we have a parser error now.
    // //? Cycle in variable initialization order

    //? Cannot use 'self' in direct member initializer.
    var a1 = evil(self);

    var b = 10;
}

fun init() {
    var e = new Example{};
}