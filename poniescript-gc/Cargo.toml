[package]
name = "poniescript-gc"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[features]
mimalloc = ["dep:mimalloc"]

[dependencies]
mimalloc = { version = "0.1.48", optional = true }
