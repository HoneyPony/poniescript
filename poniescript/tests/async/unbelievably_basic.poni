fun call_with_print(cb: fun(int)) {
    print("hello async!");
    cb(0);
}

fun test() -> int {
    call_with_print().await;
    print("yippee");
    call_with_print().await;
    print("hippo");
    return 5;
}

fun init() {
    test(fun(ret: int) {});
}