fun call_with_print(cb: fun(int)) {
    print("hello async!");
    cb(0);
}

fun test() -> int {
    call_with_print().await;
    print("yippee");
    return 5;
}

//! hello async!
//! yippee
//! final callback
fun init() {
    fun final_cb(ret: int) {
        print("final callback");
    }
    test(final_cb);
}