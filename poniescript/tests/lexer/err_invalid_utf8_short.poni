fun init() {
	// Note: For now, our lexer is too greedy and eats up the " along with the
	// invalid byte. But, this isn't really a problem, because it should handle
	// any well-formed UTF-8 correctly.

	//? Parse error: Invalid UTF-8 byte sequence: [255]
	print("ÿ");
}
