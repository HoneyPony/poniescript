fun evil(e: Example) -> int {
    return e.a1 + 20;
}

class Example {
    // This is invalid because evil() reads from a1.

    // TODO: This test case currently passes... Uh oh.

    //? Cycle in variable initialization order
    var a1 = evil(self);

    var b = 10;
}

fun init() {
    var e = new Example{};
}