class Outer {
    var property = (1, 2);

    class Inner {
        //? Identifier 'property' is not available in this scope.
        var inner_property = property + (3, 4);
    }
}

fun init() {
    var outer = new Outer{};
    var inner = new Outer.Inner{};

    print(inner.inner_property);
}