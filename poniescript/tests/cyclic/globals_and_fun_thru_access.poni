// The compiler needs to figure out that a gets initialized after
// b & c, specifically because they're both needed by test().
var a = test();
var b = 10;
var c = 20;

fun test() -> int {
    return b * c;
}

fun init() {
    print(a, " ", b, " ", c); //! 200 10 20
}