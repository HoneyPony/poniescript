fun init() {
    // Everything should promote to float because we are multiplying by float.
    //
    // This should work because we're promoting a literal, not an expression.
    print(((1, 2.0), 3) * 5.0); //! ((1.000000, 2.000000), 3.000000)
}