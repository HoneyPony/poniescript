[package]
name = "poniescript-gc"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib", "staticlib"]

[features]
mimalloc = ["dep:mimalloc"]
hotreload = []

[dependencies]
env_logger = "0.11.8"
log = "0.4.28"
mimalloc = { version = "0.1.48", optional = true }
