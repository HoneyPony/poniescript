class Base {
    var x: int = 80;
    var y: int = 90;
}

class Subclass : Base {
    var z: int = 100;
}

fun init() {
    var sub = new Subclass {};

    print(sub.x); //! 80
    print(sub.y); //! 90
    print(sub.z); //! 100
}